class EmailValidator:
    def is_valid_email(self, email):
        def is_valid_format(email):
            # Checking the valied email format 
            pass
        def is_unique(email):
            # Check the condition
            pass
        return is_valid_format(email) and is_unique(email)

validator = EmailValidator()
result = validator.is_valid_email("khaikhai@company.malik")
